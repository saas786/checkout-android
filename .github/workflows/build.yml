# Workflow for building snapshot versions of the Android Checkout SDK.

name: Build workflow

on:
  push:
    branches:
      - "PCX-1662"
jobs:
  build:
    runs-on: ubuntu-latest
    name: "Build"
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adopt'
      - name: Validate Gradle wrapper
        uses: gradle/wrapper-validation-action@v1
      - name: Clean Android Checkout SDK
        run: ./gradlew clean
      - name: Build Checkout module
        run: ./gradlew buildCheckout

  test:
    needs: build
    name: "Test"
    environment: Browserstack
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adopt'
      - name: Validate Gradle wrapper
        uses: gradle/wrapper-validation-action@v1
      - name: Run Checkout unit-tests
        run: ./gradlew testCheckout
      - name: Test example apps on Browserstack
        env:
          MOBILE_MERCHANT_CODE: ${{ secrets.MOBILEDEV_MERCHANT_CODE }}
          MOBILE_MERCHANT_PAYMENT_TOKEN: ${{ secrets.MOBILEDEV_MERCHANT_PAYMENT_TOKEN }}
          BROWSERSTACK_USER: ${{ secrets.MOBILEDEV_BROWSERSTACK_USER }}
          BROWSERSTACK_KEY: ${{ secrets.MOBILEDEV_BROWSERSTACK_KEY }}
          PAYMENTAPI_LISTURL: ${{ secrets.MOBILEDEV_PAYMENTAPI_LISTURL }}
        run: ./gradlew testExampleApps

  publish:
    needs: test
    name: "Publish"
    environment: Publish
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'
    - name: Validate Gradle wrapper
      uses: gradle/wrapper-validation-action@v1
    - name: Publish checkout module
      env:
        NEXUS_USER: ${{ secrets.MOBILEDEV_NEXUS_USER }}
        NEXUS_PASSWORD: ${{ secrets.MOBILEDEV_NEXUS_PASSWORD }}
        PACKAGECLOUD_TOKEN: ${{ secrets.MOBILEDEV_PACKAGECLOUD_TOKEN }}
      run: ./gradlew publishCheckout